language: generic

sudo: required

before_install:
  - sudo apt-get update
  - sudo apt-get install -y texlive-latex-recommended texlive-latex-extra latex2html 
  - sudo apt-get install -y build-essential 

script:
  - make
